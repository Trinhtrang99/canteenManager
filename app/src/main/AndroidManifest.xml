<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantmanager">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantManager">
        <activity android:name=".admin.ListDetailActivity"></activity>
        <activity android:name=".admin.EditActivity" /> <!-- <activity android:name=".admin.AdminActivity"></activity> -->
        <activity android:name=".account.HistoryActivity" />
        <activity android:name=".account.ChartActivity" />
        <activity
            android:name=".acc.ForgotPasswordActivity"
            android:exported="true" />
        <activity android:name=".acc.RegisActivity" />
        <activity android:name=".admin.AdminActivity">

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".acc.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>